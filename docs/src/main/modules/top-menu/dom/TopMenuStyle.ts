import { style } from 'typestyle';

export const navbarBrandClassName = style({
  display: 'flex',
  alignItems: 'center',
  marginTop: '-4px',
});

export const navbarMenuClassName = style({
  marginLeft: '30px',
});
